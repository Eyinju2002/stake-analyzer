[project]
name = "stake-analyzer"
authors = ["Stake Analyzer Team"]
description = "A comprehensive staking analytics smart contract"
telemetry = false
requirements = []
[contracts.stake-analyzer-core]
path = "contracts/stake-analyzer-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
